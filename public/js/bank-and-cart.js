// Generated by CoffeeScript 1.3.3
(function() {

  (function() {
    var domLoaded, onDomLoad, onDomUnload;
    onDomLoad = function() {
      ++domLoaded;
      if (domLoaded > 1) {
        return;
      }
      $("header#main").find(".alerts-summary").find("span[data-title^=\"Protrada\"]").attr("data-alerts", $(".protrada .alert-count").attr("data-alerts"));
      $(".go-checkout").on("click", function(e) {
        e.preventDefault();
        Panels.add({
          id: "checkout",
          url: "modules/bank-and-cart/panels/checkout",
          size: "mini",
          temporary: true,
          h1: "checkout",
          h2: "items in cart"
        }, true);
      });
      return;
      $("#temp-checkout").on("click", function(e) {
        e.preventDefault();
        console.log("temp checkout");
        Panels.add({
          id: "temp-checkout",
          url: "/modules/bank-and-cart/panels/checkout",
          temporary: true,
          h1: "temp-checkout",
          h2: "moo in here"
        }, true);
      });
      return $("#temp-export-data").on("click", function(e) {
        e.preventDefault();
        console.log("export data");
        Panels.add({
          id: "export-data",
          url: "/panels/export-data",
          temporary: true,
          h1: "export-data",
          h2: "moo in here"
        }, true);
      });
    };
    onDomUnload = function() {
      $(".ajax-spinner").show();
      $(window).off("resize", windowResize);
      $(window).off("scroll", windowScroll);
    };
    domLoaded = 0;
    $("#main-container").one({
      ajaxUnload: onDomUnload
    });
    onDomLoad();
    $(document).on("click", "#toggle-side-bar, #x-side-bar", function(e) {
      windowResize();
    });
  })();

}).call(this);
