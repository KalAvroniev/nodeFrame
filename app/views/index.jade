- if layout
	extends layouts/default

block body
	script
		// user agent sniffing is frowned upon :(
		// tim: true, but not for the reasons that we use it (mobile detection only). <http://united-coders.com/matthias-reuter/user-agent-sniffing-is-back>
		// in any case, maybe we should try one of these... (although they both still seem to use UA sniffing) <http://notnotmobile.appspot.com> or <http://www.justindocanto.com/scripts/detect-a-user-on-a-mobile-browser-or-device>
		if ( /Android|webOS|BlackBerry|iP(hone|ad|od)/i.test( navigator.userAgent ) ) {
			document.body.className += " mobile";
		}

	include partials/task-status
	include partials/backend-spine
	include partials/alerts-aside

	#ajax-container

	// footer area content
	.ajax-spinner
		.ball
		.ball1