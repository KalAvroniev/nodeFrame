!!! 5
html(lang="en")
	head
		meta(charset="utf-8")
		meta(http-equiv="X-UA-Compatible", content="IE=edge,chrome=1")
		meta(name="viewport", content="width=device-width, initial-scale=1, maximum-scale=1, user-scalable=no")
		title Protrada (Bananaman release)
		!= partial("partials/styles")

	body.sidebar-hidden
		script
			// user agent sniffing is frowned upon :(
			// tim: true, but not for the reasons that we use it (mobile detection only). http://united-coders.com/matthias-reuter/user-agent-sniffing-is-back/
			// in any case, maybe we should try one of these... (although they both still seem to use UA sniffing) http://notnotmobile.appspot.com/ or http://www.justindocanto.com/scripts/detect-a-user-on-a-mobile-browser-or-device
			if ( /Android|webOS|BlackBerry|iP(hone|ad|od)/i.test( navigator.userAgent ) ) {
				document.body.className += " mobile";
			}

		!= partial("partials/task-status")
		!= partial("partials/backend-spine")
		!= partial("partials/alerts-aside")

		#ajax-container

		// footer area content
		.ajax-spinner
			.ball
			.ball1

		script(src="/js/require-2.0.4.min.js", data-main="js/app")
		script
			var _gaq = _gaq || [];
			_gaq.push(
				[ "_setAccount", "UA-33674085-1" ],
				[ "_trackPageview" ],
				[ "_setCustomVar", 1, "Module", document.URL.substr( document.URL.lastIndexOf("/") + 1 ) || "home", 3 ]
			);

			(function() {
				var ga = document.createElement("script"); ga.type = "text/javascript"; ga.async = true;
				ga.src = ( "https:" == document.location.protocol ? "https://ssl" : "http://www" ) + ".google-analytics.com/ga.js";
				var s = document.getElementsByTagName("script")[0]; s.parentNode.insertBefore( ga, s );
			})();